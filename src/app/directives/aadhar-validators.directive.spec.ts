import { AadharValidatorsDirective } from './aadhar-validators.directive';

describe('AadharValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new AadharValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
