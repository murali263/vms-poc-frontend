import { UsernameValidatorsDirective } from './username-validators.directive';

describe('UsernameValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
